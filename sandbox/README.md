# Sandbox

Gitignored except this file. Per experiment:

```text
sandbox/<slug>/
  AGENT_LOG.md   # commands, outputs, decisions (required for agent runs)
  ...
```

Promote summaries to `docs/research/topics/`. Do not commit large artifacts here.
